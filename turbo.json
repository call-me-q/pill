{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "POSTGRES_USER",
    "POSTGRES_PASSWORD",
    "POSTGRES_DB",
    "POSTGRES_PORT",
    "POSTGRES_HOST"
  ],
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false,
      "dependsOn": ["^build-db"]
    },
    "build-constants": {
      "outputs": ["./dist/**"]
    },
    "build-db": {
      "outputs": ["./dist/**"],
      "dependsOn": ["^build-constants"]
    }
  }
}
